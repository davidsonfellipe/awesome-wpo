import { TextlintFixResult } from "@textlint/kernel";
export declare class TextLintFixer {
    /**
     * write output to each files and return promise
     * @param textFixMessages
     * @returns {Promise}
     */
    write(textFixMessages: TextlintFixResult[]): Promise<TextlintFixResult[]>;
}
